name: luna
props:
  type: object
  required:
    - color
  properties:
    dismissable:
      type: boolean
      title: Dismissable
      description: If true users can dismiss the luna by clicking on it.
    color:
      type: string
      title: Color
      description: "Background color to use for the luna, one of 'primary' or 'secondary'."
      enum: ['primary', 'secondary']
slots:
  luna_content:
      title: Content
      description: Content of the luna
